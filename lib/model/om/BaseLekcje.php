<?php

/**
 * Base class that represents a row from the 'lekcje' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * śro, 7 gru 2011, 12:49:33
 *
 * @package    lib.model.om
 */
abstract class BaseLekcje extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        LekcjePeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the przedmiot_id field.
	 * @var        int
	 */
	protected $przedmiot_id;

	/**
	 * The value for the klasa_id field.
	 * @var        int
	 */
	protected $klasa_id;

	/**
	 * The value for the semestr_id field.
	 * @var        int
	 */
	protected $semestr_id;

	/**
	 * The value for the temat field.
	 * @var        string
	 */
	protected $temat;

	/**
	 * The value for the datalekcji field.
	 * @var        string
	 */
	protected $datalekcji;

	/**
	 * The value for the nauczyciel_id field.
	 * @var        int
	 */
	protected $nauczyciel_id;

	/**
	 * @var        Przedmiot
	 */
	protected $aPrzedmiot;

	/**
	 * @var        Klasy
	 */
	protected $aKlasy;

	/**
	 * @var        Semestr
	 */
	protected $aSemestr;

	/**
	 * @var        Uzytkownik
	 */
	protected $aUzytkownik;

	/**
	 * @var        array Obecnosci[] Collection to store aggregation of Obecnosci objects.
	 */
	protected $collObecnoscis;

	/**
	 * @var        Criteria The criteria used to select the current contents of collObecnoscis.
	 */
	private $lastObecnosciCriteria = null;

	/**
	 * @var        array Uwagi[] Collection to store aggregation of Uwagi objects.
	 */
	protected $collUwagis;

	/**
	 * @var        Criteria The criteria used to select the current contents of collUwagis.
	 */
	private $lastUwagiCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'LekcjePeer';

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [przedmiot_id] column value.
	 * 
	 * @return     int
	 */
	public function getPrzedmiotId()
	{
		return $this->przedmiot_id;
	}

	/**
	 * Get the [klasa_id] column value.
	 * 
	 * @return     int
	 */
	public function getKlasaId()
	{
		return $this->klasa_id;
	}

	/**
	 * Get the [semestr_id] column value.
	 * 
	 * @return     int
	 */
	public function getSemestrId()
	{
		return $this->semestr_id;
	}

	/**
	 * Get the [temat] column value.
	 * 
	 * @return     string
	 */
	public function getTemat()
	{
		return $this->temat;
	}

	/**
	 * Get the [optionally formatted] temporal [datalekcji] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDatalekcji($format = 'Y-m-d H:i:s')
	{
		if ($this->datalekcji === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->datalekcji);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->datalekcji, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [nauczyciel_id] column value.
	 * 
	 * @return     int
	 */
	public function getNauczycielId()
	{
		return $this->nauczyciel_id;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     Lekcje The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = LekcjePeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [przedmiot_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Lekcje The current object (for fluent API support)
	 */
	public function setPrzedmiotId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->przedmiot_id !== $v) {
			$this->przedmiot_id = $v;
			$this->modifiedColumns[] = LekcjePeer::PRZEDMIOT_ID;
		}

		if ($this->aPrzedmiot !== null && $this->aPrzedmiot->getId() !== $v) {
			$this->aPrzedmiot = null;
		}

		return $this;
	} // setPrzedmiotId()

	/**
	 * Set the value of [klasa_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Lekcje The current object (for fluent API support)
	 */
	public function setKlasaId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->klasa_id !== $v) {
			$this->klasa_id = $v;
			$this->modifiedColumns[] = LekcjePeer::KLASA_ID;
		}

		if ($this->aKlasy !== null && $this->aKlasy->getId() !== $v) {
			$this->aKlasy = null;
		}

		return $this;
	} // setKlasaId()

	/**
	 * Set the value of [semestr_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Lekcje The current object (for fluent API support)
	 */
	public function setSemestrId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->semestr_id !== $v) {
			$this->semestr_id = $v;
			$this->modifiedColumns[] = LekcjePeer::SEMESTR_ID;
		}

		if ($this->aSemestr !== null && $this->aSemestr->getId() !== $v) {
			$this->aSemestr = null;
		}

		return $this;
	} // setSemestrId()

	/**
	 * Set the value of [temat] column.
	 * 
	 * @param      string $v new value
	 * @return     Lekcje The current object (for fluent API support)
	 */
	public function setTemat($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->temat !== $v) {
			$this->temat = $v;
			$this->modifiedColumns[] = LekcjePeer::TEMAT;
		}

		return $this;
	} // setTemat()

	/**
	 * Sets the value of [datalekcji] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Lekcje The current object (for fluent API support)
	 */
	public function setDatalekcji($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->datalekcji !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->datalekcji !== null && $tmpDt = new DateTime($this->datalekcji)) ? $tmpDt->format('Y-m-d\\TH:i:sO') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d\\TH:i:sO') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->datalekcji = ($dt ? $dt->format('Y-m-d\\TH:i:sO') : null);
				$this->modifiedColumns[] = LekcjePeer::DATALEKCJI;
			}
		} // if either are not null

		return $this;
	} // setDatalekcji()

	/**
	 * Set the value of [nauczyciel_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Lekcje The current object (for fluent API support)
	 */
	public function setNauczycielId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->nauczyciel_id !== $v) {
			$this->nauczyciel_id = $v;
			$this->modifiedColumns[] = LekcjePeer::NAUCZYCIEL_ID;
		}

		if ($this->aUzytkownik !== null && $this->aUzytkownik->getId() !== $v) {
			$this->aUzytkownik = null;
		}

		return $this;
	} // setNauczycielId()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->przedmiot_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->klasa_id = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
			$this->semestr_id = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->temat = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->datalekcji = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->nauczyciel_id = ($row[$startcol + 6] !== null) ? (int) $row[$startcol + 6] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 7; // 7 = LekcjePeer::NUM_COLUMNS - LekcjePeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Lekcje object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aPrzedmiot !== null && $this->przedmiot_id !== $this->aPrzedmiot->getId()) {
			$this->aPrzedmiot = null;
		}
		if ($this->aKlasy !== null && $this->klasa_id !== $this->aKlasy->getId()) {
			$this->aKlasy = null;
		}
		if ($this->aSemestr !== null && $this->semestr_id !== $this->aSemestr->getId()) {
			$this->aSemestr = null;
		}
		if ($this->aUzytkownik !== null && $this->nauczyciel_id !== $this->aUzytkownik->getId()) {
			$this->aUzytkownik = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(LekcjePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = LekcjePeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aPrzedmiot = null;
			$this->aKlasy = null;
			$this->aSemestr = null;
			$this->aUzytkownik = null;
			$this->collObecnoscis = null;
			$this->lastObecnosciCriteria = null;

			$this->collUwagis = null;
			$this->lastUwagiCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(LekcjePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseLekcje:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				LekcjePeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseLekcje:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(LekcjePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseLekcje:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseLekcje:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				LekcjePeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aPrzedmiot !== null) {
				if ($this->aPrzedmiot->isModified() || $this->aPrzedmiot->isNew()) {
					$affectedRows += $this->aPrzedmiot->save($con);
				}
				$this->setPrzedmiot($this->aPrzedmiot);
			}

			if ($this->aKlasy !== null) {
				if ($this->aKlasy->isModified() || $this->aKlasy->isNew()) {
					$affectedRows += $this->aKlasy->save($con);
				}
				$this->setKlasy($this->aKlasy);
			}

			if ($this->aSemestr !== null) {
				if ($this->aSemestr->isModified() || $this->aSemestr->isNew()) {
					$affectedRows += $this->aSemestr->save($con);
				}
				$this->setSemestr($this->aSemestr);
			}

			if ($this->aUzytkownik !== null) {
				if ($this->aUzytkownik->isModified() || $this->aUzytkownik->isNew()) {
					$affectedRows += $this->aUzytkownik->save($con);
				}
				$this->setUzytkownik($this->aUzytkownik);
			}


			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = LekcjePeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setNew(false);
				} else {
					$affectedRows += LekcjePeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collObecnoscis !== null) {
				foreach ($this->collObecnoscis as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collUwagis !== null) {
				foreach ($this->collUwagis as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aPrzedmiot !== null) {
				if (!$this->aPrzedmiot->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aPrzedmiot->getValidationFailures());
				}
			}

			if ($this->aKlasy !== null) {
				if (!$this->aKlasy->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aKlasy->getValidationFailures());
				}
			}

			if ($this->aSemestr !== null) {
				if (!$this->aSemestr->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aSemestr->getValidationFailures());
				}
			}

			if ($this->aUzytkownik !== null) {
				if (!$this->aUzytkownik->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aUzytkownik->getValidationFailures());
				}
			}


			if (($retval = LekcjePeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collObecnoscis !== null) {
					foreach ($this->collObecnoscis as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collUwagis !== null) {
					foreach ($this->collUwagis as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = LekcjePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getPrzedmiotId();
				break;
			case 2:
				return $this->getKlasaId();
				break;
			case 3:
				return $this->getSemestrId();
				break;
			case 4:
				return $this->getTemat();
				break;
			case 5:
				return $this->getDatalekcji();
				break;
			case 6:
				return $this->getNauczycielId();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = LekcjePeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getPrzedmiotId(),
			$keys[2] => $this->getKlasaId(),
			$keys[3] => $this->getSemestrId(),
			$keys[4] => $this->getTemat(),
			$keys[5] => $this->getDatalekcji(),
			$keys[6] => $this->getNauczycielId(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = LekcjePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setPrzedmiotId($value);
				break;
			case 2:
				$this->setKlasaId($value);
				break;
			case 3:
				$this->setSemestrId($value);
				break;
			case 4:
				$this->setTemat($value);
				break;
			case 5:
				$this->setDatalekcji($value);
				break;
			case 6:
				$this->setNauczycielId($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = LekcjePeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setPrzedmiotId($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setKlasaId($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setSemestrId($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setTemat($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setDatalekcji($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setNauczycielId($arr[$keys[6]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(LekcjePeer::DATABASE_NAME);

		if ($this->isColumnModified(LekcjePeer::ID)) $criteria->add(LekcjePeer::ID, $this->id);
		if ($this->isColumnModified(LekcjePeer::PRZEDMIOT_ID)) $criteria->add(LekcjePeer::PRZEDMIOT_ID, $this->przedmiot_id);
		if ($this->isColumnModified(LekcjePeer::KLASA_ID)) $criteria->add(LekcjePeer::KLASA_ID, $this->klasa_id);
		if ($this->isColumnModified(LekcjePeer::SEMESTR_ID)) $criteria->add(LekcjePeer::SEMESTR_ID, $this->semestr_id);
		if ($this->isColumnModified(LekcjePeer::TEMAT)) $criteria->add(LekcjePeer::TEMAT, $this->temat);
		if ($this->isColumnModified(LekcjePeer::DATALEKCJI)) $criteria->add(LekcjePeer::DATALEKCJI, $this->datalekcji);
		if ($this->isColumnModified(LekcjePeer::NAUCZYCIEL_ID)) $criteria->add(LekcjePeer::NAUCZYCIEL_ID, $this->nauczyciel_id);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(LekcjePeer::DATABASE_NAME);

		$criteria->add(LekcjePeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Lekcje (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setId($this->id);

		$copyObj->setPrzedmiotId($this->przedmiot_id);

		$copyObj->setKlasaId($this->klasa_id);

		$copyObj->setSemestrId($this->semestr_id);

		$copyObj->setTemat($this->temat);

		$copyObj->setDatalekcji($this->datalekcji);

		$copyObj->setNauczycielId($this->nauczyciel_id);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getObecnoscis() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addObecnosci($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getUwagis() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addUwagi($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Lekcje Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     LekcjePeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new LekcjePeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Przedmiot object.
	 *
	 * @param      Przedmiot $v
	 * @return     Lekcje The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setPrzedmiot(Przedmiot $v = null)
	{
		if ($v === null) {
			$this->setPrzedmiotId(NULL);
		} else {
			$this->setPrzedmiotId($v->getId());
		}

		$this->aPrzedmiot = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Przedmiot object, it will not be re-added.
		if ($v !== null) {
			$v->addLekcje($this);
		}

		return $this;
	}


	/**
	 * Get the associated Przedmiot object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Przedmiot The associated Przedmiot object.
	 * @throws     PropelException
	 */
	public function getPrzedmiot(PropelPDO $con = null)
	{
		if ($this->aPrzedmiot === null && ($this->przedmiot_id !== null)) {
			$this->aPrzedmiot = PrzedmiotPeer::retrieveByPk($this->przedmiot_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aPrzedmiot->addLekcjes($this);
			 */
		}
		return $this->aPrzedmiot;
	}

	/**
	 * Declares an association between this object and a Klasy object.
	 *
	 * @param      Klasy $v
	 * @return     Lekcje The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setKlasy(Klasy $v = null)
	{
		if ($v === null) {
			$this->setKlasaId(NULL);
		} else {
			$this->setKlasaId($v->getId());
		}

		$this->aKlasy = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Klasy object, it will not be re-added.
		if ($v !== null) {
			$v->addLekcje($this);
		}

		return $this;
	}


	/**
	 * Get the associated Klasy object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Klasy The associated Klasy object.
	 * @throws     PropelException
	 */
	public function getKlasy(PropelPDO $con = null)
	{
		if ($this->aKlasy === null && ($this->klasa_id !== null)) {
			$this->aKlasy = KlasyPeer::retrieveByPk($this->klasa_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aKlasy->addLekcjes($this);
			 */
		}
		return $this->aKlasy;
	}

	/**
	 * Declares an association between this object and a Semestr object.
	 *
	 * @param      Semestr $v
	 * @return     Lekcje The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setSemestr(Semestr $v = null)
	{
		if ($v === null) {
			$this->setSemestrId(NULL);
		} else {
			$this->setSemestrId($v->getId());
		}

		$this->aSemestr = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Semestr object, it will not be re-added.
		if ($v !== null) {
			$v->addLekcje($this);
		}

		return $this;
	}


	/**
	 * Get the associated Semestr object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Semestr The associated Semestr object.
	 * @throws     PropelException
	 */
	public function getSemestr(PropelPDO $con = null)
	{
		if ($this->aSemestr === null && ($this->semestr_id !== null)) {
			$this->aSemestr = SemestrPeer::retrieveByPk($this->semestr_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aSemestr->addLekcjes($this);
			 */
		}
		return $this->aSemestr;
	}

	/**
	 * Declares an association between this object and a Uzytkownik object.
	 *
	 * @param      Uzytkownik $v
	 * @return     Lekcje The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setUzytkownik(Uzytkownik $v = null)
	{
		if ($v === null) {
			$this->setNauczycielId(NULL);
		} else {
			$this->setNauczycielId($v->getId());
		}

		$this->aUzytkownik = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Uzytkownik object, it will not be re-added.
		if ($v !== null) {
			$v->addLekcje($this);
		}

		return $this;
	}


	/**
	 * Get the associated Uzytkownik object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Uzytkownik The associated Uzytkownik object.
	 * @throws     PropelException
	 */
	public function getUzytkownik(PropelPDO $con = null)
	{
		if ($this->aUzytkownik === null && ($this->nauczyciel_id !== null)) {
			$this->aUzytkownik = UzytkownikPeer::retrieveByPk($this->nauczyciel_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aUzytkownik->addLekcjes($this);
			 */
		}
		return $this->aUzytkownik;
	}

	/**
	 * Clears out the collObecnoscis collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addObecnoscis()
	 */
	public function clearObecnoscis()
	{
		$this->collObecnoscis = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collObecnoscis collection (array).
	 *
	 * By default this just sets the collObecnoscis collection to an empty array (like clearcollObecnoscis());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initObecnoscis()
	{
		$this->collObecnoscis = array();
	}

	/**
	 * Gets an array of Obecnosci objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Lekcje has previously been saved, it will retrieve
	 * related Obecnoscis from storage. If this Lekcje is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Obecnosci[]
	 * @throws     PropelException
	 */
	public function getObecnoscis($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(LekcjePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collObecnoscis === null) {
			if ($this->isNew()) {
			   $this->collObecnoscis = array();
			} else {

				$criteria->add(ObecnosciPeer::LEKCJA_ID, $this->id);

				ObecnosciPeer::addSelectColumns($criteria);
				$this->collObecnoscis = ObecnosciPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ObecnosciPeer::LEKCJA_ID, $this->id);

				ObecnosciPeer::addSelectColumns($criteria);
				if (!isset($this->lastObecnosciCriteria) || !$this->lastObecnosciCriteria->equals($criteria)) {
					$this->collObecnoscis = ObecnosciPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastObecnosciCriteria = $criteria;
		return $this->collObecnoscis;
	}

	/**
	 * Returns the number of related Obecnosci objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Obecnosci objects.
	 * @throws     PropelException
	 */
	public function countObecnoscis(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(LekcjePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collObecnoscis === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ObecnosciPeer::LEKCJA_ID, $this->id);

				$count = ObecnosciPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ObecnosciPeer::LEKCJA_ID, $this->id);

				if (!isset($this->lastObecnosciCriteria) || !$this->lastObecnosciCriteria->equals($criteria)) {
					$count = ObecnosciPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collObecnoscis);
				}
			} else {
				$count = count($this->collObecnoscis);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Obecnosci object to this object
	 * through the Obecnosci foreign key attribute.
	 *
	 * @param      Obecnosci $l Obecnosci
	 * @return     void
	 * @throws     PropelException
	 */
	public function addObecnosci(Obecnosci $l)
	{
		if ($this->collObecnoscis === null) {
			$this->initObecnoscis();
		}
		if (!in_array($l, $this->collObecnoscis, true)) { // only add it if the **same** object is not already associated
			array_push($this->collObecnoscis, $l);
			$l->setLekcje($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Lekcje is new, it will return
	 * an empty collection; or if this Lekcje has previously
	 * been saved, it will retrieve related Obecnoscis from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Lekcje.
	 */
	public function getObecnoscisJoinUzytkownik($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(LekcjePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collObecnoscis === null) {
			if ($this->isNew()) {
				$this->collObecnoscis = array();
			} else {

				$criteria->add(ObecnosciPeer::LEKCJA_ID, $this->id);

				$this->collObecnoscis = ObecnosciPeer::doSelectJoinUzytkownik($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ObecnosciPeer::LEKCJA_ID, $this->id);

			if (!isset($this->lastObecnosciCriteria) || !$this->lastObecnosciCriteria->equals($criteria)) {
				$this->collObecnoscis = ObecnosciPeer::doSelectJoinUzytkownik($criteria, $con, $join_behavior);
			}
		}
		$this->lastObecnosciCriteria = $criteria;

		return $this->collObecnoscis;
	}

	/**
	 * Clears out the collUwagis collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addUwagis()
	 */
	public function clearUwagis()
	{
		$this->collUwagis = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collUwagis collection (array).
	 *
	 * By default this just sets the collUwagis collection to an empty array (like clearcollUwagis());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initUwagis()
	{
		$this->collUwagis = array();
	}

	/**
	 * Gets an array of Uwagi objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Lekcje has previously been saved, it will retrieve
	 * related Uwagis from storage. If this Lekcje is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Uwagi[]
	 * @throws     PropelException
	 */
	public function getUwagis($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(LekcjePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collUwagis === null) {
			if ($this->isNew()) {
			   $this->collUwagis = array();
			} else {

				$criteria->add(UwagiPeer::LEKCJA_ID, $this->id);

				UwagiPeer::addSelectColumns($criteria);
				$this->collUwagis = UwagiPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(UwagiPeer::LEKCJA_ID, $this->id);

				UwagiPeer::addSelectColumns($criteria);
				if (!isset($this->lastUwagiCriteria) || !$this->lastUwagiCriteria->equals($criteria)) {
					$this->collUwagis = UwagiPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastUwagiCriteria = $criteria;
		return $this->collUwagis;
	}

	/**
	 * Returns the number of related Uwagi objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Uwagi objects.
	 * @throws     PropelException
	 */
	public function countUwagis(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(LekcjePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collUwagis === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(UwagiPeer::LEKCJA_ID, $this->id);

				$count = UwagiPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(UwagiPeer::LEKCJA_ID, $this->id);

				if (!isset($this->lastUwagiCriteria) || !$this->lastUwagiCriteria->equals($criteria)) {
					$count = UwagiPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collUwagis);
				}
			} else {
				$count = count($this->collUwagis);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Uwagi object to this object
	 * through the Uwagi foreign key attribute.
	 *
	 * @param      Uwagi $l Uwagi
	 * @return     void
	 * @throws     PropelException
	 */
	public function addUwagi(Uwagi $l)
	{
		if ($this->collUwagis === null) {
			$this->initUwagis();
		}
		if (!in_array($l, $this->collUwagis, true)) { // only add it if the **same** object is not already associated
			array_push($this->collUwagis, $l);
			$l->setLekcje($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Lekcje is new, it will return
	 * an empty collection; or if this Lekcje has previously
	 * been saved, it will retrieve related Uwagis from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Lekcje.
	 */
	public function getUwagisJoinUzytkownik($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(LekcjePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collUwagis === null) {
			if ($this->isNew()) {
				$this->collUwagis = array();
			} else {

				$criteria->add(UwagiPeer::LEKCJA_ID, $this->id);

				$this->collUwagis = UwagiPeer::doSelectJoinUzytkownik($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(UwagiPeer::LEKCJA_ID, $this->id);

			if (!isset($this->lastUwagiCriteria) || !$this->lastUwagiCriteria->equals($criteria)) {
				$this->collUwagis = UwagiPeer::doSelectJoinUzytkownik($criteria, $con, $join_behavior);
			}
		}
		$this->lastUwagiCriteria = $criteria;

		return $this->collUwagis;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collObecnoscis) {
				foreach ((array) $this->collObecnoscis as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collUwagis) {
				foreach ((array) $this->collUwagis as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collObecnoscis = null;
		$this->collUwagis = null;
			$this->aPrzedmiot = null;
			$this->aKlasy = null;
			$this->aSemestr = null;
			$this->aUzytkownik = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseLekcje:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseLekcje::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseLekcje
